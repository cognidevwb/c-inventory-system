# INVMGR.CFG — site settings. key=value, no spaces around '='.
# Missing keys fall back to factory defaults. Unknown keys are ignored.
COMPANY=CONSOLIDATED WAREHOUSE SUPPLY
REPORT_WIDTH=80
# DATA_DIR=/data/invmgr
